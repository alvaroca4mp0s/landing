# ── OS ──
.DS_Store
.DS_Store?
._*
Thumbs.db

# ── Editores / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Dependencias ──
node_modules/

# ── Next.js / build ──
/.next/
/out/
/build/
next-env.d.ts
*.tsbuildinfo

# ── Tests / cobertura ──
/coverage/
.vitest/

# ── Entorno y secretos ──
# Ignora TODOS los .env excepto la plantilla pública.
.env
.env.*
!.env.example

# ── Leads persistidos (fallback del CRM) — nunca versionar ──
/data/

# ── Vercel ──
.vercel

# ── Claude Code / notas locales de sesión ──
.claude/settings.local.json
resume_claude.txt
*.resume.txt

# ── Remotion (mantener fuente, ignorar generados/deps en cualquier ruta) ──
**/remotion/node_modules/
**/remotion/.cache/
**/remotion/dist/
**/remotion/out/
**/remotion/.tmp/

# ── Logs ──
*.log
npm-debug.log*

# ── Salida de deploy ──
dist/
